{{ define "title"}}<title>سیستم تصمیم‌گیری دلفی</title>{{ end }}

{{ define "content" }}

<div class="page-header">
  <h2 id="title">در حال بارگزاری</h2>
  <h3 id="compareQuestion"></h3>
</div>

<div class="row" id="items">
  <div class="col-sm-6">
    <button id="item1" type="button" class="btn btn-default btn-lg btn-block">&nbsp;</button>
  </div>
  <div class="col-sm-6">
    <button id="item2" type="button" class="btn btn-default btn-lg btn-block">&nbsp;</button>
  </div>
</div>

{{ end }}

{{ define "scripts" }}
	<script src="/s/js/delphi.js"></script>
{{ end }}
